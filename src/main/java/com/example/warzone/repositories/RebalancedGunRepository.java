package com.example.warzone.repositories;

import com.example.warzone.models.RebalancedGun;
import org.springframework.data.jpa.repository.JpaRepository;


public interface RebalancedGunRepository extends JpaRepository<RebalancedGun, Long> {
}
